POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# --- Host port mappings ----------------------------------------------------
DB_PORT=

# Base URL the browser uses to reach the API (as seen from the host).
VITE_API_URL=

# Browser origins the API accepts (comma-separated).
CORS_ORIGINS=